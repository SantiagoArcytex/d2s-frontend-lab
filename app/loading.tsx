import { LogoPageLoader } from '@/components/feedback/LogoPageLoader';

/**
 * Root segment loading UI. Shown during initial load and when navigating to /.
 */
export default function RootLoading() {
  return <LogoPageLoader />;
}
